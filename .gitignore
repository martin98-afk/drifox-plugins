# Python
__pycache__/
*.py[co]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# 临时调试/探测脚本（connection error 根因排查等一次性脚本）
.test/

# 虚拟环境
.venv/
venv/
env/

# 编辑器 / IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# 钩子运行时产物（Evolver 等插件可能产生）
memory/
*.jsonl
!plugins/*/skills/**/*.jsonl

# 临时文件
*.log
*.tmp
*.bak

# 凭据 / 配置
.env
.env.local
*.pem
*.key

# ── Auto-appended by format_memory_context hook ──

# Python 生态
*.pyc
*.pyo
*.egg
build/
dist/
*.spec

# 系统文件
desktop.ini
$RECYCLE.BIN/

# IDE/编辑器
*.swn
.project
.pycpath

# ��������ʱ����
.drifox/


# ssh-toolkit vendored deps: keep Windows compiled extensions (.pyd) tracked
!plugins/ssh-toolkit/deps/*.pyd
!plugins/ssh-toolkit/deps/**/*.pyd

# 网关插件 vendor 的 certifi CA 包：被上方 `*.pem` 误伤，必须随插件分发（公共证书，非密钥）。
# 否则分发后 deps/certifi/ 缺 cacert.pem → certifi.where() 返回不存在路径 →
# ssl.create_default_context(cafile=...) 抛 FileNotFoundError，且因 sys.path.insert(0, deps)
# 全局抢占主程序 certifi，导致所有 OpenAI/HTTPS 调用崩溃。
!plugins/gateway-dingtalk/deps/certifi/cacert.pem
!plugins/gateway-feishu/deps/certifi/cacert.pem
!plugins/gateway-telegram/deps/certifi/cacert.pem
# 网关插件用户配置（含 App Secret 等凭据，禁止入库）
plugins/*/config.json
