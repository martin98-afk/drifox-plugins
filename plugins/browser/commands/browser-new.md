---
description: 在浏览器中新建标签页
type: function
shortcut: Ctrl+T
---

# /browser-new — 新建标签页

打开浏览器并新建一个空白标签页（若浏览器未打开则先打开）。

- 全局快捷键：Ctrl+T（与 Chrome 一致；浏览器内 Ctrl+T 由 QShortcut 处理，等效行为）

> 这是 `type: function` 命令：由 Python 处理器直接执行，不经过 AI。
