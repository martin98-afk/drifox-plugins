# 技能包契约模板

## 结果定义
- 用户：
- 压力场景：
- 最终产物：
- 3 分钟可见结果：
- 成功标准：
- 非目标：
- 一票否决项：

## 目标清晰度
- 谁在什么压力下使用：
- 最终获得的可见产物或状态改变：
- 可观察的成功条件：
- 明确不包含的相邻结果：
- 两个执行者是否会对“完成”产生不同理解：

## 领域研究契约
- 原始用户意图：
- 可能领域：
- 研究强度：`minimum-sweep` / `deep-research`
- 待支持的关键决策：
- 研究问题：
- 来源层级：
- 已读证据：
- 时效 / 日期边界：
- 冲突证据处理：
- 关键发现：
- 结论如何改变设计：
- 停止规则：
- 用户 / 角色：
- 压力时刻：
- 既有工作流：
- 原生产物：
- 工作表面 / 工具：
- 本地能力清单：
- 优先工具 / MCP / 脚本路线：
- 输出证据路线：
- 第一判断标准：
- 必须真实提供的输入：
- 可以生成的部分：
- 不得模拟的部分：
- 反证：
- 决定：`make` / `do-not-make` / `research-needed`
- 写 `SKILL.md` 前的停止条件：

## 体验表面契约
- 在领域研究契约之后填写：
- 平台 / 工作表面：
- 原生媒介：
- 第一个可见产物：
- 完整最终包：
- 受众判断标准：
- 输入/主题到产物的链路：
- 必须是真实用户材料：
- 可以生成：
- 不得模拟：

## 本地能力 / 多模态契约
- 非纯文本、渲染结果或生成文件必须填写：
- 当前宿主工具：
- MCP / 插件工具：
- 既有本地脚本 / 资产：
- 成本或授权边界：
- 优先路线：
- 降级路线：
- 是否需要结构化简报：
- 必须保留的输出证据：
- 生成无法运行时的声明边界：

## 触发契约
- 应触发：
- 不应触发：
- 近似但不触发：
- 模糊输入处理：
- `description` 修改规则：

## 输入 / 输出契约
- 必填输入：
- 可选输入：
- 信息缺失时行为：
- 输出产物：
- 文件或可见结果：

## 执行步骤契约

步骤数量由真实工作流决定；每一步都填写以下字段：

| 步骤 | 进入条件 / 输入 | 可执行动作 | 可观察输出 / 状态 | 转移条件 | 失败 / 降级路线 |
|---|---|---|---|---|---|
| 1 |  |  |  |  |  |

- 从输入到最终产物是否形成连续链：
- 是否存在只有“分析 / 优化 / 处理”但无法验收的空动作：
- 是否存在不改变产物或后续决策的多余步骤：

## 包契约
- `SKILL.md` 包含：
- `references/` 包含：
- `scripts/` / `assets/` / 模板包含：
- 多模态提示词简报包含：
- `examples/` 包含：
- `evals/` 包含：

## 边界契约
- 范围边界：支持场景、非目标、相邻 Skill 分工：
- 事实边界：必须真实提供、允许生成、不得模拟：
- 权限边界：删除、发布、付费、隐私、外部写入的确认要求：
- 副作用边界：会修改的文件、系统、账户或外部状态：
- 停止边界：何时必须停止或等待：
- 降级边界：降级后能交付什么、不能再声明什么：

## 边界完整性
- scope / truth / authority / side effect / stop / fallback 是否全部覆盖：
- 是否存在与相邻 Skill 重叠但没有路由规则的场景：
- 是否存在越权后再补确认的步骤：

## 评测契约
- 目标清晰度评测：
- 执行步骤清晰度评测：
- 边界完整性评测：
- 深度研究契约评测：
- 触发评测：
- 输出评测：
- 与无 Skill 基线对比：
- 版本 A/B：
- 人工复查：
- 漂移信号：
